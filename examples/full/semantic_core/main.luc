# The executable semantic slice: a selective import, a call across modules,
# a binding, a comparison, and `if` — everything the interpreter runs today.
# `luce run --package org.luce.examples main.answer ...` prints 42.
from math import double

pub func answer() -> i64:
    let value = double(21)
    if value == 42: return value
    return 0
