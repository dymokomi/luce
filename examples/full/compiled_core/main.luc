# Integer arithmetic in the compiled slice. Every backend encodes these
# operations; a WebAssembly module exports `answer`, while a native executable
# still needs a `main`.
pub func answer() -> i64: return 6 * 7
