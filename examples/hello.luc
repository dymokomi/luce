# The smallest native executable in the compiled slice: constant terminal
# output and an `i32` exit status from `main`. Also builds as WebAssembly.
pub func main(arguments: slice[str]) -> i32:
    print("Hello, world!")
    return 0
