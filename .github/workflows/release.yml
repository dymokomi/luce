name: Release
# A tag `luce-VERSION` on a green main becomes a GitHub release with one archive per host:
# `luce` with the Base compiler bootstrap/BASE names bundled beside it and Base's standard
# library, each proved by tools/install_smoke.sh before it is published: what the
# installers at luce.luciaos.com download.
on:
  push:
    tags: ['luce-*']
  workflow_dispatch:
permissions:
  contents: write
jobs:
  unix:
    name: Package (${{ matrix.host }})
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: macos-15
            host: arm64-macos
          # the oldest supported runner, so the compilers run on that glibc and newer
          - runner: ubuntu-22.04
            host: x86_64-linux
          - runner: ubuntu-22.04-arm
            host: arm64-linux
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v7
      - name: Check the tag names this version
        if: github.ref_type == 'tag'
        run: |
          [ "${GITHUB_REF_NAME#luce-}" = "$(tr -d '[:space:]' < VERSION)" ] || { echo "the tag does not name VERSION"; exit 1; }
      - name: Build with the pinned Base compiler
        run: LUCE_BASE_SOURCE=https://github.com/dymokomi/luce-base.git ./build.sh
      - name: Fetch luc to bundle in the archive
        run: git clone --depth 1 https://github.com/dymokomi/luce-luc.git ../luce-luc
      - name: Package
        run: tools/package.sh build/release
      - name: Prove the archive
        run: tools/install_smoke.sh build/release/luce-*-${{ matrix.host }}.tar.gz build/smoke
      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.host }}
          path: build/release/luce-*
  windows:
    name: Package (x86_64-windows)
    runs-on: windows-2025
    timeout-minutes: 90
    defaults:
      run:
        shell: msys2 {0}
    env:
      CC: gcc
    steps:
      - uses: actions/checkout@v7
        with:
          path: luce
      - name: Read the Base pin
        id: pins
        shell: bash
        run: echo "base=$(cat luce/bootstrap/BASE)" >> "$GITHUB_OUTPUT"
      - uses: actions/checkout@v7
        with:
          repository: dymokomi/luce-base
          ref: ${{ steps.pins.outputs.base }}
          path: luce-base
      - uses: msys2/setup-msys2@v2
        with:
          msystem: UCRT64
          update: true
          install: >-
            mingw-w64-ucrt-x86_64-gcc
            mingw-w64-ucrt-x86_64-python
            git
      - name: Check the tag names this version
        if: github.ref_type == 'tag'
        working-directory: luce
        run: |
          [ "${GITHUB_REF_NAME#luce-}" = "$(tr -d '[:space:]' < VERSION)" ] || { echo "the tag does not name VERSION"; exit 1; }
      - name: Build Base
        working-directory: luce-base
        run: python tools/build_windows.py
      - name: Build Luce
        working-directory: luce
        run: python tools/build_windows.py
      - name: Fetch luc to bundle in the archive
        working-directory: luce
        run: git clone --depth 1 https://github.com/dymokomi/luce-luc.git ../luce-luc
      - name: Package
        working-directory: luce
        run: sh tools/package.sh build/release
      - name: Prove the archive
        working-directory: luce
        run: sh tools/install_smoke.sh build/release/luce-*-x86_64-windows.tar.gz build/smoke
      - uses: actions/upload-artifact@v4
        with:
          name: x86_64-windows
          path: luce/build/release/luce-*
  publish:
    name: Publish the release
    needs: [unix, windows]
    if: github.ref_type == 'tag'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: release
          merge-multiple: true
      - name: Create or update the GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release view "$GITHUB_REF_NAME" --repo "$GITHUB_REPOSITORY" > /dev/null 2>&1 || \
            gh release create "$GITHUB_REF_NAME" --repo "$GITHUB_REPOSITORY" --title "$GITHUB_REF_NAME" --notes "luce ${GITHUB_REF_NAME#luce-} with its Base compiler bundled: one archive per host, installed by https://luce.luciaos.com."
          gh release upload "$GITHUB_REF_NAME" --repo "$GITHUB_REPOSITORY" --clobber release/luce-*
