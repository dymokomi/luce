#==============================================================================================
#
#   core_types - Source spellings of language-defined types
#
#   DESCRIPTION:
#       The reserved type names from the 1.0 surface. The parser uses them to
#       tell `foo[T](` from `foo[0](`; the checker uses them so a known-but-
#       unimplemented type is "not implemented yet".
#
#==============================================================================================

## Report whether `name` is a language-defined type spelling, implemented or not.
pub func is_core_type_name(name: str) -> bool:
    return match name:
        "bool", "u8", "u16", "u32", "u64", "i8", "i16", "i32", "i64" => true
        "f16", "f32", "f64", "char", "str", "bytes", "unit", "never" => true
        "array", "slice", "mutable_slice", "list", "map", "set", "range", "task" => true
        else => false
