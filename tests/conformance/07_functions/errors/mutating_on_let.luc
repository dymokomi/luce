# error: changes its receiver, which must be a `var`
struct C:
    var n: int
    func bump(self):
        self.n = self.n + 1
pub func main(arguments: list[str]) -> int!:
    let c = C(n = 1)
    c.bump()
    return 0
