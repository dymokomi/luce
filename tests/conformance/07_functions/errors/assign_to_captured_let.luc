# error: 5:9: `base` is a `let` and cannot be assigned; declare it `var`
pub func main(arguments: list[str]) -> int!:
    let base = 1
    let bump = func ():
        base = 2
    bump()
    return 0
