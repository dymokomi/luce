# error: 3:32: the lambda is used as a `func(int) -> bool`, which takes another number of parameters
pub func main(arguments: list[str]) -> int!:
    let f: func(int) -> bool = (a, b) => a > b
    return 0
