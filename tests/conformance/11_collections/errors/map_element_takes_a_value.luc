# error: none
## §11.2: `m[key] = v` inserts or replaces a value; `none` is not one, `remove` takes a key out.
pub func main(arguments: list[str]) -> int!:
    var ages: map[str, int] = {}
    ages["a"] = none
    return 0
