# error: 6:17: a map's keys are equatable and hashable, which a `Box` is not (§11.2)
class Box:
    var n: int = 0

pub func main(arguments: list[str]) -> int!:
    let boxes = {Box(): 1}
    return 0
