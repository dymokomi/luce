# error: 7:15: `list[Box]` has no ordering on its elements (§4.4)
class Box:
    var n: int = 0

pub func main(arguments: list[str]) -> int!:
    var boxes = [Box(), Box()]
    boxes.sort()
    return 0
