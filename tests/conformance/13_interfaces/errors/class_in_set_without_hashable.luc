# error: 9:16: a set's elements are equatable and hashable, which a `Account` is not (§11.2)
class Account: Equatable:
    let id: int = 0

    func equals(self, other: Account) -> bool:
        return self.id == other.id

pub func main(arguments: list[str]) -> int!:
    let seen = {Account()}
    return 0
