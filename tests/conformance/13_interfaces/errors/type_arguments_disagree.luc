# error: 2:1: the type parameter `T` is `int` from one argument and `str` from another
func pair[T](a: T, b: T) -> (T, T):
    return (a, b)

pub func main(arguments: list[str]) -> int!:
    print(pair(1, "one"))
    return 0
