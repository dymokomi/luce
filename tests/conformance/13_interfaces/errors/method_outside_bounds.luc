# error: 6:14: a `T` has only what its bounds require; no `area`
interface Shape:
    func area(self) -> float

func measure[T](x: T) -> float:
    return x.area()

pub func main(arguments: list[str]) -> int!:
    return 0
