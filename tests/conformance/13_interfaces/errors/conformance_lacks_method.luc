# error: 5:16: the conformance to this interface lacks the method `area`
interface Shape:
    func area(self) -> float

struct Square: Shape:
    var side: float

pub func main(arguments: list[str]) -> int!:
    return 0
