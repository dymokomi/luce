# error: 6:13: `identity` is generic: it is called with its type arguments known, not read as a value (§13.2)
func identity[T](x: T) -> T:
    return x

pub func main(arguments: list[str]) -> int!:
    let f = identity
    return 0
