# error: 6:17: cannot iterate a `Box`; it does not declare `Iterable` (§8.3)
struct Box:
    var value: int

pub func main(arguments: list[str]) -> int!:
    for x in Box(value = 1):
        print(x)
    return 0
