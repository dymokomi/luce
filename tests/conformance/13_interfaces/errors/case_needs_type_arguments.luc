# error: 7:20: the type parameter `T` of `Option` is not fixed here; write the type arguments, `Option[int]`
enum Option[T]:
    some(value: T)
    empty

pub func main(arguments: list[str]) -> int!:
    let e = Option.empty
    return 0
