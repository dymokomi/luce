# error: 8:5: the method `area` does not have the signature the interface requires
interface Shape:
    func area(self) -> float

struct Square: Shape:
    var side: float

    func area(self) -> int:
        return 1

pub func main(arguments: list[str]) -> int!:
    return 0
