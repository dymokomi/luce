# error: 9:27: this value does not conform to `Shape`
interface Shape:
    func area(self) -> float

struct Dot:
    var x: int

pub func main(arguments: list[str]) -> int!:
    let shape: Shape = Dot(x = 1)
    return 0
