# error: 5:5: a method takes no type parameters of its own; the type does (§13.2)
struct Box:
    var n: int

    func wrap[T](self, x: T) -> T:
        return x

pub func main(arguments: list[str]) -> int!:
    return 0
