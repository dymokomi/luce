# error: 2:14: `Equatable` is a closed protocol: a type declares it, no value has it as its type (§13.3)
func same(a: Equatable) -> bool:
    return true

pub func main(arguments: list[str]) -> int!:
    return 0
