# error: 9:12: `Square` does not have `Ordered`, which `T` requires (§13.2)
struct Square:
    var side: float

struct Sorted[T: Ordered]:
    var items: list[T]

pub func main(arguments: list[str]) -> int!:
    let s: Sorted[Square] = Sorted(items = [])
    return 0
