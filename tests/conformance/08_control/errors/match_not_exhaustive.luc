# error: this match does not cover the case `empty`
enum Shape:
    circle(r: float)
    empty
pub func main(arguments: list[str]) -> int!:
    let s = Shape.circle(r = 1.0)
    match s:
        .circle(r):
            print(r)
    return 0
