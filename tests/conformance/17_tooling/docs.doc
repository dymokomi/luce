# docs

## struct Point
A point on the plane.
Two integers.

- x: int
  Across.

- y: int

- func moved(self, dx: int) -> Point
  The point moved along x.

## enum Side
The sides of a coin.

- case heads
  Heads up.

- case tails

## interface Shape
What a shape can say of itself.

- func area(self) -> float
  Its area.

## let origin: Point
The origin.

## func inc(n: int) -> int
Adds one.

## func main(arguments: list[str]) -> int!
