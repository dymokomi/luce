# error: 10:7: the field `title` is a `let`; it is assigned once, in `init`
class Document:
    let title: str

    func init(self, title: str):
        self.title = title

pub func main(arguments: list[str]) -> int!:
    let d = Document("a")
    d.title = "b"
    return 0
