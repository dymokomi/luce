# error: 15:28: `deinit` does not publish `self`; it reads and calls its own members (§10.3)
class Registry:
    var count: int = 0

    pub func note(self, other: Registry):
        self.count += 1

class Item:
    let registry: Registry

    func init(self, registry: Registry):
        self.registry = registry

    func deinit(self):
        self.registry.note(self)

pub func main(arguments: list[str]) -> int!:
    return 0
