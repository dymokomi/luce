# error: 7:13: a `Box` is never `none`; only an optional is compared with `none`
class Box:
    var n: int = 0

pub func main(arguments: list[str]) -> int!:
    let a = Box()
    print(a is none)
    return 0
