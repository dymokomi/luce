# error: 6:5: `init` does not assign the field `body` on every path (§10.1)
class Message:
    let title: str
    let body: str

    func init(self, title: str):
        self.title = title

pub func main(arguments: list[str]) -> int!:
    return 0
