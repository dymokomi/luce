# error: 8:13: a class has identity, not equality: compare with `is`, or declare `Equatable` (§4.4)
class Box:
    var n: int = 0

pub func main(arguments: list[str]) -> int!:
    let a = Box()
    let b = Box()
    print(a == b)
    return 0
