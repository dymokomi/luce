# error: 9:19: the task's result, a `Counter`, is not sendable (§14)
class Counter:
    var n: int = 0

func make() -> Counter:
    return Counter()

pub func main(arguments: list[str]) -> int!:
    let t = spawn make()
    return 0
