# error: 6:5: a task is waited by the function that spawned it; a closure does not capture it (§14)
func answer() -> int:
    return 1

pub func main(arguments: list[str]) -> int!:
    let t = spawn answer()
    let later = () => wait t
    return later()
