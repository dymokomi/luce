# error: 6:16: a task is waited by the function that spawned it; nothing holds one (§14)
func answer() -> int:
    return 1

pub func main(arguments: list[str]) -> int!:
    var tasks: list[task[int]] = []
    return 0
