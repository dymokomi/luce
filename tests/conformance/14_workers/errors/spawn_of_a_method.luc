# error: 9:30: `spawn` starts a call of a named function: a method or a closure would share what a worker may not (§14)
struct Box:
    var n: int

    func get(self) -> int:
        return self.n

pub func main(arguments: list[str]) -> int!:
    let t = spawn Box(n = 1).get()
    return wait t
