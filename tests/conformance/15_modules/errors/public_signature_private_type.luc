# error: 5:5: the public `reveal` mentions the private type `Secret` (§15.2)
struct Secret:
    var n: int

pub func reveal() -> Secret:
    return Secret(n = 1)

pub func main(arguments: list[str]) -> int!:
    return 0
