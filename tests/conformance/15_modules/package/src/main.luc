# §15.5: `package.prisma` names the package and its source root; modules are named from there.
from util.math import twice

pub func main(arguments: list[str]) -> int!:
    print(twice(21))
    return 0
