# error: `T!` belongs in a function result; store a `Result[T]` instead
func value() -> int!:
    return 7

pub func main(arguments: list[str]) -> int!:
    let pending: int! = value()
    return try pending
