# error: a `catch` suite ends in `recover`, `return`, or `error`
func f() -> int!:
    return 1
pub func main(arguments: list[str]) -> int!:
    let x = f() catch failure:
        print(failure.message)
    return 0
