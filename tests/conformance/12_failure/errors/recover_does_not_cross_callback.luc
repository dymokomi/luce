# error: `recover` belongs in a `catch` suite
func failing() -> int!:
    return 7

func example() -> int:
    return failing() catch failure:
        let operation = func() -> int:
            recover 0
        recover operation()

pub func main(arguments: list[str]) -> int!:
    return example()
