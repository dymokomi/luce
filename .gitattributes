# GitHub only loads grammars installed globally through Linguist, so until
# Luce is upstreamed there (see github.com/dymokomi/luce-grammar), borrow
# Python's highlighting — the closest visual match: # comments,
# colon-and-indent blocks, f-strings.
*.luc linguist-language=Python
